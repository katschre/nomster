<!-- this would just show every single detail of the code -->
<!-- we want to break this up to show what we want, how we want -->
<!-- #<h1><%= @places.inspect %></h1> -->

<% @places.each do |place| %>
  <div class="booyah-box col-xs-10 col-xs-offset-1">
    <h1><%= link_to place.name, place_path(place) %></h1><br />
    <i><%= place.address %></i><br /><br />
    <p><%= place.description %></p><br />

    <% if place.user.present? %>
      <small><%= place.user.email %></small>
    <% end %>

  <% if place.comments.present? %>
    <div class="col-xs-12">
      <blockquote>
                    <% if place.comments.present? && place.comments.last.humanized_rating.present? %>
                        <b><%= place.comments.last.humanized_rating %> </b>-
                    <% end %>

                    <% if place.comments.present?%>
                        <%= place.comments.last.message %>
                        <% if place.comments.last.user.present? %>
                            <small><%= place.comments.last.user.email %></small>
                        <% end %>
                    <% end %>
      </blockquote>
    </div>
  <% end %>

  <% if place.photos.present? %>
    <div class="col-xs-12 col-sm-6">
        <%= image_tag place.photos.last.avatar %>
        <small><%= place.photos.last.caption %></small>
    </div>
  <% end %>

  </div>
<% end %>